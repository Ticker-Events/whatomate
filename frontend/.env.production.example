# Production UI build — copy to .env.production (gitignored) or export before build.
# API/WebSocket use same-origin paths in production; only Firebase client config is required.
#
# Used by: digitalocean/scripts/build-and-push.sh
# Baked into the SPA at docker build time (see frontend/Dockerfile).

# Minified JSON on one line
VITE_FIREBASE_CONFIG=
