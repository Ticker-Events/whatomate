VITE_API_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws

# Firebase client config (optional — enables Firestore real-time chat updates)
# Minified JSON on one line (from Firebase console → Project settings → Your apps → Web app config)
VITE_FIREBASE_CONFIG=
